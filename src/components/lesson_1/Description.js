import React from "react";

const Description = () => {
  return (
    <div>
      <p>In this example we learn about, list components, conditional rendering, wrap component styles, forms, two way binding, useState Hook and other silly things.</p>
    </div>
  );
};

export default Description;
